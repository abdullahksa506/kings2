# 🤝 دليل التعاون على الموقع

هذا المستند يشرح كيف نشتغل أنا وصاحبي على الموقع بدون ما نخرب على بعض.

> **الفرع الرسمي هو `main`** — الموقع على Render ينشر منه. لا أحد يكتب على `main` مباشرة؛ كل التغييرات تدخل عبر **Pull Request**.

---

## 🥇 القاعدة الذهبية

**قبل ما تبدأ أي شغل جديد، اسحب آخر نسخة من `main`.**

```bash
git checkout main
git pull origin main
git checkout -b <اسم-الفرع>
```

كذا تشتغل دايماً على أحدث نسخة، والتعارضات تقل لأقصى حد.

---

## 📂 توزيع الشغل (مهم)

| المطوّر | المسؤولية |
|---------|-----------|
| **عبدالله** (عبر Claude) | الخريطة، الاقتراحات، باقي الميزات |
| **الصديق** (عبر VS Code) | المتجر |

**لا تشتغلون الاثنين على نفس الملف بنفس الوقت.** لو احتجتم تلمسون نفس الملف، نسّقوا: "أنا ماسك هذا الملف اليوم."

---

## 🔄 طريقة العمل خطوة بخطوة (VS Code)

```bash
# 1. ابدأ من نسخة محدّثة
git checkout main
git pull origin main

# 2. افتح فرع لميزتك (اسم واضح)
git checkout -b feature/store

# 3. اشتغل... ثم احفظ
git add .
git commit -m "وصف واضح للتغيير"

# 4. ارفع الفرع
git push -u origin feature/store

# 5. افتح Pull Request من GitHub → دمج في main
```

### تسمية الفروع
- `feature/...` لميزة جديدة (مثل `feature/store`)
- `fix/...` لإصلاح خطأ (مثل `fix/map-marker`)

---

## ⚠️ الملفات "المشتركة" (أكثر مصدر للتعارض)

هذي الملفات يلمسها الطرفان عادةً. عشان نتجنب التعارض، **كل واحد يضيف كوده في مكان مختلف** (قسم خاص بميزته):

| الملف | الغرض |
|------|-------|
| `src/lib/services.ts` | دوال العميل (كل واحد يضيف دواله بالآخر) |
| `src/app/api/rpc/route.ts` | أوامر السيرفر (كل واحد يضيف `case` خاص بميزته) |
| `src/components/Dashboard.tsx` | ربط الميزات بالواجهة |

**نصيحة:** أضف كودك في **نهاية** القسم، وحُط تعليق يوضّح ميزتك:
```ts
// ===== ميزة المتجر (الصديق) =====
...
// ===== نهاية ميزة المتجر =====
```

---

## 🔧 لو صار تعارض (Conflict)

ما هو نهاية العالم — يصير لما الطرفان يعدّلان **نفس السطر**. الحل:

```bash
# وأنت على فرعك، اسحب main
git pull origin main

# لو طلع تعارض، Git يأشّر الأماكن في الملف هكذا:
<<<<<<< HEAD
كودك أنت
=======
كود الطرف الثاني
>>>>>>> main

# افتح الملف، اختر الصح (أو ادمج الاثنين)، احذف أسطر <<< === >>>
git add <الملف>
git commit
git push
```

> لو أنت تشتغل عبر Claude، قل له "فيه تعارض حله" وهو يحله لك.

---

## ✅ قبل أي دمج

1. تأكد المشروع يبني بدون أخطاء:
   ```bash
   npx tsc --noEmit     # صفر أخطاء TypeScript
   ```
2. جرّب ميزتك تشتغل محلياً (`npm run dev`).
3. افتح PR، واقرأ التغييرات قبل الدمج.

---

## 🆘 أوامر إنقاذ سريعة

| الموقف | الأمر |
|--------|-------|
| أبي أرجع نسختي لآخر نسخة نظيفة | `git checkout main && git pull origin main` |
| خربت ملف وأبي أرجّعه | `git checkout -- <الملف>` |
| أبي أشوف وش تغيّر | `git status` و `git diff` |
| أبي أشوف آخر التحديثات | `git fetch origin && git log origin/main --oneline -10` |

---

## 📌 خلاصة في 4 نقاط

1. **اسحب `main` قبل ما تبدأ.**
2. **فرع لكل ميزة + PR لكل تغيير.**
3. **لا تلمسون نفس الملف بنفس الوقت.**
4. **تأكد `npx tsc --noEmit` نظيف قبل الدمج.**
